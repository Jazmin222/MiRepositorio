numero = int(input("Ingrese el número para el que desea calcular los múltiplos: "))
limite = int(input("Ingrese el límite: "))
suma = 0
 
for n in range(1, limite + 1):
    if n % numero == 0:
        suma += n
 
print("La suma de los múltiplos de", numero, "hasta", limite, "es:", suma)
